# install librariesnya dulu!

di terminal:
``` terminal
pip install -r "training_requirements.txt"
```

atau run ini kalo mau install di notebook 
``` notebook
!pip install -r "./../training_requirements.txt"
```

note: kalo run local (di VS Code) jangan lupa install ipykernel